import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Gaps = ({width, height}) => {
    return (
        <View style={{width, height}} />
    )
}

export default Gaps;

